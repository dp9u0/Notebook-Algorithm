/**
 * 
 * @param {number[]} d : 距离
 * @param {number} n : 学校数量
 */
function build_school(d, n) {
  let dist = [];
  let length = d.length;
  console.log(dist);
}

let d, n;
d = [2, 4, 6, 5, 2, 4, 3, 1, 3], n = 3;
build_school(d, n)