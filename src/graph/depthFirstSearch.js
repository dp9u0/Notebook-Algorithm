/* eslint-disable no-unused-vars */
const {
  Graph
} = require('./Graph');

const DefaultCallbacks = {

}

/**
 * dfs graph
 * @param {Graph} graph graph to search
 * @param {Object} callbacks callbacks when traverse
 */
function dfs(graph, callbacks = DefaultCallbacks) {

}

module.exports = dfs;