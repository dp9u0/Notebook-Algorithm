/**
 * ShortestPath
 * @typedef  {Object} Point point index 
 * @property {number} x point
 * @property {number} y point
 */

/**
 * A Star Path Finder
 * @param {number[][]} map matrix for map
 * @param {Point} start start point
 * @param {Point} end end point
 * @returns {Point[]} path
 */
function astar(map, start, end) {
  let paths = []
  return paths;
}

module.exports = astar;